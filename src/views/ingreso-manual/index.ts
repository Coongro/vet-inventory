/**
 * Ingreso manual — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `ingreso-manual.view.ts`,
 * datos en `use-ingreso-manual.ts`, lógica custom en `handlers.ts`.
 */
export { IngresoManualView } from './ingreso-manual.view.js';
