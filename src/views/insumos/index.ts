/**
 * Insumos — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `insumos.view.ts`,
 * datos en `use-insumos.ts`, lógica custom en `handlers.ts`.
 */
export { InsumosView } from './insumos.view.js';
