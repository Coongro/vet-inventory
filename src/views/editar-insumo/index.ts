/**
 * Editar insumo — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `editar-insumo.view.ts`,
 * datos en `use-editar-insumo.ts`, lógica custom en `handlers.ts`.
 */
export { EditarInsumoView } from './editar-insumo.view.js';
