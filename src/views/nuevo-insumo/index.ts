/**
 * Nuevo insumo — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `nuevo-insumo.view.ts`,
 * datos en `use-nuevo-insumo.ts`, lógica custom en `handlers.ts`.
 */
export { NuevoInsumoView } from './nuevo-insumo.view.js';
