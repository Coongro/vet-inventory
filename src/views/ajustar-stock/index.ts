/**
 * Dar de baja — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `ajustar-stock.view.ts`,
 * datos en `use-ajustar-stock.ts`, lógica custom en `handlers.ts`.
 */
export { AjustarStockView } from './ajustar-stock.view.js';
